import React from "react";
import { createRoot } from "react-dom/client";
import GraceGatherEventsWebsite from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GraceGatherEventsWebsite />
  </React.StrictMode>
);
